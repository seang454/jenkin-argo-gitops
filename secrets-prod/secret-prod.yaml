---
apiVersion: v1
kind: Namespace
metadata:
  name: prod

---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-prod-secret
  namespace: prod
type: Opaque
stringData:
  postgres-username: "appuser"
  postgres-password: "MyStr0ngPass!"   # ⚠️ change this
  postgres-database: "appdb_prod"

---
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-db-secret
  namespace: prod
type: Opaque
stringData:
  db-password: "MyStr0ngPass!"         # ⚠️ must match postgres-prod-secret password

---
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-admin-secret
  namespace: prod
type: Opaque
stringData:
  admin-password: "MyStr0ngPass!"      # ⚠️ change this
---
# ──────────────────────────────────────────
# Cloudflare DNS credentials
# Used by:
#   - templates/cloudflare-dns-job.yaml  (creates DNS records via Helm hook)
#   - templates/clusterissuer.yaml       (cert-manager DNS01 challenge)
# ──────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-secret          # must match cloudflare.secretName in values.yaml
  namespace: prod
type: Opaque
stringData:
  token: "2KxUVBNL0VX_t5D-5BtkMuW7agYp1lxFTAjEYsKO"   # ⚠️ Cloudflare Dashboard → My Profile → API Tokens
  zoneId: "25794f1056c9f59652052d977bd73acb"    # ⚠️ Cloudflare Dashboard → seang.shop → Overview → Zone ID
